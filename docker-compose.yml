version: '3.8'

services:
  summarizer:
    build: .
    container_name: local-doc-summarizer
    ports:
      - "8501:8501"
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped